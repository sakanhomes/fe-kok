// axios timeout
export const AXIOS_TIMEOUT = 1000 * 60

// api
export const API_REST_URL = `${process.env.NEXT_PUBLIC_API_REST_URL}/`
export const API_UPLOAD_URL = `${process.env.NEXT_PUBLIC_API_UPLOAD_URL}`

// app
export const APP_URL = `${process.env.NEXT_PUBLIC_APP_URL}`

// alchemy
export const ALCHEMY_ID = `${process.env.NEXT_PUBLIC_ALCHEMY_ID}`
